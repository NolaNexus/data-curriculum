---
type: lab
module: Foundations
timebox_minutes: 120
status: planned
created: 2026-01-19
tags: [course/lab, topic/python]
---

# Lab objective
Create your course project repo scaffold (this becomes your capstone repo).

# Steps
1. Create repo with:
   - `pyproject.toml`
   - `src/`
   - `tests/`
   - `Makefile` or `justfile`
2. Add ruff + formatting.
3. Add one tiny function + one pytest test.
4. Make `make test` pass (or `just test`).

# Success criteria
- [ ] `pytest` passes
- [ ] lint/format runs
- [ ] README contains install + first run steps

# Evidence
```text
```
